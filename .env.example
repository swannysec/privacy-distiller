# Privacy Policy Analyzer - Environment Variables
#
# Copy this file to .env.local for local development
# For production (GitHub Pages), set these as repository variables in:
# Settings → Secrets and variables → Actions → Variables tab

# Enable the hosted free tier LLM proxy
# Set to "true" to show the "Hosted Free" provider option
VITE_FREE_TIER_ENABLED=false

# URL of your deployed Cloudflare Worker (free tier proxy)
# Example: https://privacy-policy-free-tier.your-subdomain.workers.dev
VITE_FREE_TIER_WORKER_URL=https://your-worker.your-subdomain.workers.dev

# Cloudflare Turnstile site key (public, safe to expose in client)
# Get this from: https://dash.cloudflare.com/?to=/:account/turnstile
# Use the test key below for local development (always passes):
VITE_TURNSTILE_SITE_KEY=1x00000000000000000000AA

# Free tier LLM model (optional - defaults to nvidia/nemotron-3-nano-30b-a3b:free)
# Must be a model available on OpenRouter that supports Zero Data Retention (ZDR)
# See: https://openrouter.ai/models for available models
# VITE_FREE_TIER_MODEL=nvidia/nemotron-3-nano-30b-a3b:free
